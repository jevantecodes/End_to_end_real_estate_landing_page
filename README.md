# Command Center Landing Page

Static landing page for the fix-and-flip preconstruction service.

## Local preview

Open [`index.html`](./index.html) directly in a browser, or run a simple static server from this folder:

```bash
python3 -m http.server 4173
```

Then visit `http://127.0.0.1:4173`.
